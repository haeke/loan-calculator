import React from 'react';

const ErrorMessage = () => {
    return (
        <div>
            <p className="alert alert-danger">Error - Please check your values</p>
        </div>
    );
};

export default ErrorMessage;